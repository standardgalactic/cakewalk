# GPT Adapter

Use the files in `protocol/` as the primary system-level reasoning substrate.

A GPT-specific wrapper should:
- summarize the core ontology
- instruct the model to preserve history when relevant
- direct specialized tasks through the protocol workflows
- keep adapter instructions thin and derivative
